# Antigravity Project Rules (AGENTS.md)

이 파일은 `C:\전일도` 워크스페이스 내의 개발 및 프로젝트 특정 규칙을 정의한다.

## 🛠️ 1. 개발 환경 스택

*   **OS**: Windows 11
*   **언어**: Python 3.13 (네이티브 Windows 환경 선호)
*   **비전 AI**: YOLO, OpenCV, PyTorch. 
*   **하드웨어 가속**: AMD RX 6600 (8GB VRAM) 타겟. (PyTorch DirectML 등 활용 시 텐서 및 모델 호환성에 주의)
*   **WSL2 상태**: 현재 BIOS Hyper-V / SVM 비활성화로 인해 `Ubuntu-24.04` 구동에 제약(`HCS_E_HYPERV_NOT_INSTALLED`)이 있으므로, 가급적 **Windows Native** 런타임 환경에서 작업하고 패키지를 설치한다.

## 📁 2. 주요 프로젝트 분리 (Workspace Separation)

*   **Obsidian 지식 베이스**: `C:\전일도` (개념 정리, 수업 노트, AI 시스템 등 지식의 원천)
*   **Vision AI 코스워크/개발**: `D:\KH_AI` (실제 코드, 데이터셋, 모델, 프로젝트 실행)
    *   **절대 분리 원칙**: `KH_AI` 내부의 코드/데이터셋을 `Obsidian` Vault로 복사/중복 저장하지 않습니다.
    *   `Obsidian`에서 프로젝트를 참조할 때는 `D:\KH_AI\...` 경로를 링크하는 방식을 사용합니다.

## 🌐 3. 3대 기기(DESKTOP, LAPTOP, ACADEMY) 동기화 프로토콜

*   **기기 식별**: 각 기기는 `.local/machine.json` 파일 내 `machine_id` (예: `DESKTOP`)로 식별되며, 해당 폴더는 Git 추적에서 제외(`.gitignore`)됩니다.
*   **동기화 원칙 (3-Way Merge & Conflict Protection)**:
    1.  **최초 기준**: 본체의 완성된 Obsidian Vault를 최초 기준점(Baseline)으로 삼습니다.
    2.  **동등한 권한**: 이후에는 DESKTOP, LAPTOP, ACADEMY 어느 쪽도 우선하지 않고 모두 동등한 클라이언트로 간주합니다.
    3.  **덮어쓰기/삭제 금지**: 단순 시간순 덮어쓰기나 한쪽의 변경을 임의 삭제하는 병합을 절대 수행하지 않습니다.
    4.  **충돌(Conflict) 보호**: 로컬 변경과 원격(Remote) 변경이 동시 다발적으로 발생하여 충돌이 예상될 경우, 자동 병합을 중지(STOP)하고 두 변경사항을 모두 보존한 상태에서 사용자 확인을 대기합니다.
    5.  **커밋 명시**: Git 커밋 시 `[DESKTOP] ...` 형태로 작성 주체(Machine ID)를 명시하여 변경 이력을 투명하게 관리합니다.

## ✍️ 4. 코드 작성 및 AI 코딩 원칙 (카파시 바이브 코딩 4대 원칙)

1.  **군더더기(Over-engineering) 배제**: 요청받지 않은 과도한 추상화, 불필요한 보일러플레이트 코드, 복잡한 디자인 패턴 추가 금지.
2.  **외과수술적 최소 수정 (Surgical Edit)**: 전체 파일을 덮어쓰지 않고, 수정이 필요한 라인만 `replace_file_content`로 정밀하게 핀셋 수정한다.
3.  **자가치유 검증 하네스 (Test-First)**: 코드 작성 후 터미널에서 실제 빌드·테스트를 자가 치유 루프로 통과(초록불)한 최종 결과만 보고한다. "이렇게 해보세요" 식의 추측성 코드 지양.
4.  **가정·트레이드오프 선공개**: AI가 임의로 추측해 구현하기 전에 핵심 설계 전제와 장단점을 사용자에게 먼저 명확히 공유하고 진행한다.

## 🛡️ 5. Git 및 안전 원칙

*   **데이터 파괴적 명령 사전 격리**: `rm -rf`, DB `DROP`/`TRUNCATE` 등 데이터 손실 유발 명령은 임의 실행을 차단하고 항상 사용자의 사전 승인을 얻는다.
*   **Git 안전 원칙**: 게임 관련 리소스나 `.env` 시크릿 파일 등은 철저히 `.gitignore` 처리 후 업로드(`git push`)를 방지한다. 명시적인 `/업데이트` 호출 전까지는 임의 커밋을 수행하지 않는다.
